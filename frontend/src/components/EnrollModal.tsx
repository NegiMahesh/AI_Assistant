type Props = { open: boolean; name: string; status: string; loading: boolean; onNameChange: (value: string) => void; onKeyDown: ( event: React.KeyboardEvent<HTMLInputElement> ) => void; onEnroll: () => void; onClose: () => void; }; export default function EnrollModal({ open, name, status, loading, onNameChange, onKeyDown, onEnroll, onClose, }: Props) { if ( !open ) { return null; } return ( <div className="modal-backdrop" onMouseDown={(event) => { if ( event.target === event.currentTarget && !loading ) { onClose(); } }} > <div className="enroll-modal"> <div className="enroll-modal-glow" /> <div className="enroll-header"> <div> <div className="enroll-kicker"> FACE DATABASE </div> <h2> Enroll Identity </h2> <p> Add the current person to your local InsightFace database. </p> </div> <button className="modal-close" type="button" onClick={ onClose } disabled={ loading } > ✕ </button> </div> <div className="enroll-visual"> <div className="scan-ring ring-one" /> <div className="scan-ring ring-two" /> <div className="scan-ring ring-three" /> <div className="scan-face"> 👤 </div> <span> CAMERA FRAME </span> </div> <label className="enroll-label"> Person name </label> <input className="enroll-input" type="text" value={ name } placeholder="Enter a name..." autoFocus disabled={ loading } onChange={ (event) => onNameChange( event.target.value ) } onKeyDown={ onKeyDown } /> <div className="enroll-status-box"> { status || "Keep exactly one face visible and centered." } </div> <div className="enroll-actions"> <button className="enroll-submit" type="button" onClick={ onEnroll } disabled={ loading || !name.trim() } > { loading ? "🧠 Processing..." : "✓ Enroll Face" } </button> <button className="enroll-cancel" type="button" onClick={ onClose } disabled={ loading } > Cancel </button> </div> </div> </div> ); }