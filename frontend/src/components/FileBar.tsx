type Props = { fileName: string; characters: number; loading: boolean; onRemove: () => void; }; export default function FileBar({ fileName, characters, loading, onRemove, }: Props) { if (!fileName) { return null; } return (<div className="file-bar"> <div className="file-icon"> 📄 </div> <div className="file-details"> <strong> {fileName} </strong> <span> {loading ? "Reading document..." : `${characters.toLocaleString()} characters loaded`} </span> </div> <div className="file-ready"> {loading ? "..." : "READY"} </div> <button className="file-remove" type="button" onClick={onRemove} disabled={loading} > ✕ </button> </div>); }