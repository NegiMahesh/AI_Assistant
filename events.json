[
    {
        "id": 1,
        "title": "package com.system.order; import java.util.*; import java.util.concurrent.*; import java.util.concurrent.atomic.AtomicInteger; // Custom Checked Exception class InventoryException extends Exception { public InventoryException(String message) { super(message); } } // Custom Unchecked Exception class PaymentException extends RuntimeException { public PaymentException(String message) { super(message); } } // Core Data Models class Product { private final String id; private final String name; private final double price; private int stock; public Product(String id, String name, double price, int stock) { this.id = id; this.name = name; this.price = price; this.stock = stock; } public String getId() { return id; } public String getName() { return name; } public double getPrice() { return price; } public synchronized int getStock() { return stock; } public synchronized void reduceStock(int quantity) throws InventoryException { if (this.stock < quantity) { throw new InventoryException(\"Insufficient stock for: \" + name); } this.stock -= quantity; } } class Order { private final String orderId; private final Map<Product, Integer> items; private final String customerId; private double totalAmount; public Order(String orderId, String customerId) { this.orderId = orderId; this.customerId = customerId; this.items = new HashMap<>(); } public void addItem(Product product, int quantity) { items.put(product, quantity); totalAmount += product.getPrice() * quantity; } public String getOrderId() { return orderId; } public Map<Product, Integer> getItems() { return items; } public double getTotalAmount() { return totalAmount; } } // Thread-safe Generic Cache for Processing Metrics class MetricsCache<K, V> { private final Map<K, V> map = new ConcurrentHashMap<>(); private final List<K> orderHistory = new CopyOnWriteArrayList<>(); public void put(K key, V value) { map.put(key, value); orderHistory.add(key); } public V get(K key) { return map.get(key); } public List<K> getHistory() { return Collections.unmodifiableList(orderHistory); } } // Payment Processing Service class PaymentProcessor { public static boolean process(Order order) { System.out.println(\"[Payment] Processing total $\" + order.getTotalAmount() + \" for Order: \" + order.getOrderId()); if (order.getTotalAmount() > 1000.0) { throw new PaymentException(\"Transaction limit exceeded for high-value orders.\"); } return true; } } // Main System Engine public class OrderProcessingEngine { private final Map<String, Product> inventory = new ConcurrentHashMap<>(); private final MetricsCache<String, String> metrics = new MetricsCache<>(); private final ExecutorService threadPool = Executors.newFixedThreadPool(4); private final AtomicInteger processedCount = new AtomicInteger(0); public void initInventory() { inventory.put(\"P1\", new Product(\"P1\", \"Laptop\", 1200.00, 10)); inventory.put(\"P2\", new Product(\"P2\", \"Smartphone\", 600.00, 20)); inventory.put(\"P3\", new Product(\"P3\", \"Headphones\", 150.00, 50)); } public Future<Boolean> submitOrder(final Order order) { return threadPool.submit(new Callable<Boolean>() { @Override public Boolean call() throws Exception { System.out.println(\"[Engine] Starting workflow for Order: \" + order.getOrderId()); // ERROR 1: Compilation error. Iterating over map items and modifying stock throws checked exception inside lambda/loop order.getItems().forEach((product, qty) -> { product.reduceStock(qty); }); // Deducting stock directly via sequence for (Map.Entry<Product, Integer> entry : order.getItems().entrySet()) { entry.getKey().reduceStock(entry.getValue()); } // Process Payment boolean paymentSuccess = PaymentProcessor.process(order); if (paymentSuccess) { metrics.put(order.getOrderId(), \"SUCCESS\"); processedCount.incrementAndGet(); System.out.println(\"[Engine] Order \" + order.getOrderId() + \" completed successfully.\"); return true; } return false; } }); } // ERROR 2: Compilation error. Method signature claims to return a List but lacks a return statement at the end public List<String> getProcessedOrderIds() { List<String> history = metrics.getHistory(); System.out.println(\"Fetching tracking history containing \" + history.size() + \" records.\"); } public void shutdown() { threadPool.shutdown(); try { if (!threadPool.awaitTermination(5, TimeUnit.SECONDS)) { threadPool.shutdownNow(); } } catch (InterruptedException e) { threadPool.shutdownNow(); Thread.currentThread().interrupt(); } } public static void main(String[] args) { OrderProcessingEngine engine = new OrderProcessingEngine(); engine.initInventory(); // Create standard order Order order1 = new Order(\"ORD-001\", \"CUST-A\"); order1.addItem(engine.inventory.get(\"P2\"), 2); // 2 Smartphones = $1200 order1.addItem(engine.inventory.get(\"P3\"), 1); // 1 Headphone = $150 // ERROR 3: Runtime Error (NullPointerException). P4 does not exist in inventory. order1.addItem(engine.inventory.get(\"P4\"), 5); // Create a high value order to trigger PaymentException Order order2 = new Order(\"ORD-002\", \"CUST-B\"); order2.addItem(engine.inventory.get(\"P1\"), 1); // 1 Laptop = $1200 List<Future<Boolean>> futures = new ArrayList<>(); futures.add(engine.submitOrder(order1)); futures.add(engine.submitOrder(order2)); for (Future<Boolean> future : futures) { try { // ERROR 4: Compilation error. Handled exceptions must match or catch ExecutionException/InterruptedException boolean result = future.get(); System.out.println(\"Execution tracking result: \" + result); } catch (Exception e) { System.err.println(\"Task execution failed with exception: \" + e.getMessage()); } } // ERROR 5: Compilation Error. Cannot access private final field directly from static context. System.out.println(\"Total engine processes handled: \" + engine.processedCount); engine.shutdown(); } }tell me the error in this code and corect it",
        "datetime": "2026-09-23T12:24:00",
        "created_at": "2026-09-23T12:24:05.260825"
    },
    {
        "id": 2,
        "title": "package com.system.order; import java.util.*; import java.util.concurrent.*; import java.util.concurrent.atomic.AtomicInteger; // Custom Checked Exception class InventoryException extends Exception { public InventoryException(String message) { super(message); } } // Custom Unchecked Exception class PaymentException extends RuntimeException { public PaymentException(String message) { super(message); } } // Core Data Models class Product { private final String id; private final String name; private final double price; private int stock; public Product(String id, String name, double price, int stock) { this.id = id; this.name = name; this.price = price; this.stock = stock; } public String getId() { return id; } public String getName() { return name; } public double getPrice() { return price; } public synchronized int getStock() { return stock; } public synchronized void reduceStock(int quantity) throws InventoryException { if (this.stock < quantity) { throw new InventoryException(\"Insufficient stock for: \" + name); } this.stock -= quantity; } } class Order { private final String orderId; private final Map<Product, Integer> items; private final String customerId; private double totalAmount; public Order(String orderId, String customerId) { this.orderId = orderId; this.customerId = customerId; this.items = new HashMap<>(); } public void addItem(Product product, int quantity) { items.put(product, quantity); totalAmount += product.getPrice() * quantity; } public String getOrderId() { return orderId; } public Map<Product, Integer> getItems() { return items; } public double getTotalAmount() { return totalAmount; } } // Thread-safe Generic Cache for Processing Metrics class MetricsCache<K, V> { private final Map<K, V> map = new ConcurrentHashMap<>(); private final List<K> orderHistory = new CopyOnWriteArrayList<>(); public void put(K key, V value) { map.put(key, value); orderHistory.add(key); } public V get(K key) { return map.get(key); } public List<K> getHistory() { return Collections.unmodifiableList(orderHistory); } } // Payment Processing Service class PaymentProcessor { public static boolean process(Order order) { System.out.println(\"[Payment] Processing total $\" + order.getTotalAmount() + \" for Order: \" + order.getOrderId()); if (order.getTotalAmount() > 1000.0) { throw new PaymentException(\"Transaction limit exceeded for high-value orders.\"); } return true; } } // Main System Engine public class OrderProcessingEngine { private final Map<String, Product> inventory = new ConcurrentHashMap<>(); private final MetricsCache<String, String> metrics = new MetricsCache<>(); private final ExecutorService threadPool = Executors.newFixedThreadPool(4); private final AtomicInteger processedCount = new AtomicInteger(0); public void initInventory() { inventory.put(\"P1\", new Product(\"P1\", \"Laptop\", 1200.00, 10)); inventory.put(\"P2\", new Product(\"P2\", \"Smartphone\", 600.00, 20)); inventory.put(\"P3\", new Product(\"P3\", \"Headphones\", 150.00, 50)); } public Future<Boolean> submitOrder(final Order order) { return threadPool.submit(new Callable<Boolean>() { @Override public Boolean call() throws Exception { System.out.println(\"[Engine] Starting workflow for Order: \" + order.getOrderId()); // ERROR 1: Compilation error. Iterating over map items and modifying stock throws checked exception inside lambda/loop order.getItems().forEach((product, qty) -> { product.reduceStock(qty); }); // Deducting stock directly via sequence for (Map.Entry<Product, Integer> entry : order.getItems().entrySet()) { entry.getKey().reduceStock(entry.getValue()); } // Process Payment boolean paymentSuccess = PaymentProcessor.process(order); if (paymentSuccess) { metrics.put(order.getOrderId(), \"SUCCESS\"); processedCount.incrementAndGet(); System.out.println(\"[Engine] Order \" + order.getOrderId() + \" completed successfully.\"); return true; } return false; } }); } // ERROR 2: Compilation error. Method signature claims to return a List but lacks a return statement at the end public List<String> getProcessedOrderIds() { List<String> history = metrics.getHistory(); System.out.println(\"Fetching tracking history containing \" + history.size() + \" records.\"); } public void shutdown() { threadPool.shutdown(); try { if (!threadPool.awaitTermination(5, TimeUnit.SECONDS)) { threadPool.shutdownNow(); } } catch (InterruptedException e) { threadPool.shutdownNow(); Thread.currentThread().interrupt(); } } public static void main(String[] args) { OrderProcessingEngine engine = new OrderProcessingEngine(); engine.initInventory(); // Create standard order Order order1 = new Order(\"ORD-001\", \"CUST-A\"); order1.addItem(engine.inventory.get(\"P2\"), 2); // 2 Smartphones = $1200 order1.addItem(engine.inventory.get(\"P3\"), 1); // 1 Headphone = $150 // ERROR 3: Runtime Error (NullPointerException). P4 does not exist in inventory. order1.addItem(engine.inventory.get(\"P4\"), 5); // Create a high value order to trigger PaymentException Order order2 = new Order(\"ORD-002\", \"CUST-B\"); order2.addItem(engine.inventory.get(\"P1\"), 1); // 1 Laptop = $1200 List<Future<Boolean>> futures = new ArrayList<>(); futures.add(engine.submitOrder(order1)); futures.add(engine.submitOrder(order2)); for (Future<Boolean> future : futures) { try { // ERROR 4: Compilation error. Handled exceptions must match or catch ExecutionException/InterruptedException boolean result = future.get(); System.out.println(\"Execution tracking result: \" + result); } catch (Exception e) { System.err.println(\"Task execution failed with exception: \" + e.getMessage()); } } // ERROR 5: Compilation Error. Cannot access private final field directly from static context. System.out.println(\"Total engine processes handled: \" + engine.processedCount); engine.shutdown(); } }currect this code",
        "datetime": "2026-09-23T12:25:00",
        "created_at": "2026-09-23T12:25:12.375979"
    }
]